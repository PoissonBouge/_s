/**
 * The @wordpress/scripts package handles the dependencies and default configuration for webpack and Babel.
 * The scripts package expects the source file to compile to be found at src/index.js,
 * and will save the compiled output to build/index.js.
 */

// code js local
import './js/navigation';

// code css local
// import './sass/style.scss';
